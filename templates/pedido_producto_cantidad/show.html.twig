{% extends 'base.html.twig' %}

{% block title %}PedidoProductoCantidad{% endblock %}

{% block body %}
    <h1>PedidoProductoCantidad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pedido_producto_cantidad.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ pedido_producto_cantidad.cantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pedido_producto_cantidad_index') }}">back to list</a>

    <a href="{{ path('pedido_producto_cantidad_edit', {'id': pedido_producto_cantidad.id}) }}">edit</a>

    {{ include('pedido_producto_cantidad/_delete_form.html.twig') }}
{% endblock %}