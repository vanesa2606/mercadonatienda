{% extends 'base.html.twig' %}

{% block title %}Cliente{% endblock %}

{% block body %}
    <h1>Cliente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cliente.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cliente.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cliente_index') }}">Lista</a>

    <a href="{{ path('cliente_edit', {'id': cliente.id}) }}">Editar</a>

    {{ include('cliente/_delete_form.html.twig') }}
{% endblock %}